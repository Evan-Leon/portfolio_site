# OS
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code — ignore local state but keep skills trackable
.claude/*
!.claude/skills/

# docs/ is tracked in full (session logs, specs, plans, roadmaps, wireframes —
# see AGENTS.md). A blanket `docs/*` ignore lived here 2026-07-23 → 2026-08-25
# and silently kept the swiper-migration spec/plan out of git; do not reinstate.

# Superpowers
.superpowers/
.worktrees/

# Docker
docker-compose.override.yml

# Local env (compose var substitution) — commit .env.example instead
.env

# Node (if any tooling is added)
node_modules/
theater/dist/
theater/test-results/
theater/playwright-report/
theater/.vite/

# Python bytecode (setup-validation script)
__pycache__/
*.pyc

# Misc
*.log

# Card screenshots written by scripts/art/check.mjs — run artefacts, never sources.
# Written next to the CWD check.mjs is run from, so both spellings are ignored.
cards/
scripts/art/cards/
