---
name: adding-project-screenshots
description: Use when adding, updating, or troubleshooting portfolio project screenshots — images/<slug>/ files and the two swiper references (index.html card, projects/<slug>.html detail).
---

Read `../../../.claude/skills/adding-project-screenshots/SKILL.md` in full; it
is the canonical source of truth. Keep `images/<slug>/` files in sync with BOTH
swiper references (homepage card in `index.html`, detail page in
`projects/<slug>.html`). Resolve any repo paths it mentions from the repo root.
